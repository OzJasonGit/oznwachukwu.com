const scroll = document.getElementById("SCROLL-DIV");

scroll.addEventListener(onpageshow, )


function intervalScroll(){
    
    let up = 0
    setInterval(scroll, 5000)
    up += 100;
    scroll.style.up = "%"

}